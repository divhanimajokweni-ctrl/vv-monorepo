{
  "name": "@ubuntu/realtime",
  "private": true,
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}